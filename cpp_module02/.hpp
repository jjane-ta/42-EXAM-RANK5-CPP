#pragma once

#include <iostream>
#include "ATarget.hpp"

class  : public ATarget
{
    public:
        (void);
        virtual ~(void);
        virtual  * clone(void) const;




    protected:


    private:

        static std::string baseType;

        ( const & other);
         & operator = ( const & other);
};
